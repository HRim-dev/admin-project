<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hrimDev.login.dao.LoginMapper">
    <select id="getLoginUserInfo" parameterType="String" resultType="UserDto">
        SELECT USER_ID,
               NAME
        FROM USERS
        WHERE USER_ID = #{userId}
    </select>

    <select id="getLoginRequest" parameterType="String" resultType="LoginRequest">
        SELECT USER_ID,
               PASSWORD
        FROM USERS
        WHERE USER_ID = #{userId}
    </select>

    <select id="getLoginFailCount" parameterType="String" resultType="Integer">
        SELECT COALESCE(LOGIN_FAIL_COUNT, 0) AS LOGIN_FAIL_COUNT
        FROM USERS
        WHERE USER_ID = #{userId}
    </select>

    <update id="increaseLoginFailCount" parameterType="String">
        UPDATE USERS
        SET LOGIN_FAIL_COUNT = LOGIN_FAIL_COUNT + 1
        WHERE USER_ID = #{userId}
    </update>

    <update id="resetLoginFailCount" parameterType="String">
        UPDATE USERS
        SET LOGIN_FAIL_COUNT = 0
        WHERE USER_ID = #{userId}
    </update>
</mapper>